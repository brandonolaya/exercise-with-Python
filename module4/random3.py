##Use the random library's choice() method to get a
# random element from the list of names below, and
# store the chosen name in a variable called lotery

from random import *
nombres = ["Carlos", "Julia", "Nicole", "Laura", "Mailen"]
lotery = choice(nombres)